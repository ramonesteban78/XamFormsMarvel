<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="XamFormsMarvel.Views.CharacterTemplate">
	<ContentView.Content>
		<StackLayout Spacing="0" Padding="0">
			<Grid Style="{DynamicResource CardStyle}">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="90"/>
					<ColumnDefinition Width="*"/>
					<ColumnDefinition Width="5"/>
				</Grid.ColumnDefinitions>
				<Image Source="{Binding Thumbnail}" Grid.Column="0"></Image>
				<StackLayout Grid.Column="1" Spacing="0" Padding="0,5">
					<Label Text="{Binding Name}" 
						HorizontalOptions="FillAndExpand" 
						VerticalOptions="FillAndExpand" 
						VerticalTextAlignment="Start" 
						HorizontalTextAlignment="Start"
						Style="{DynamicResource NameStyle}"></Label>
				</StackLayout>
			</Grid>
		</StackLayout>
	</ContentView.Content>
</ContentView>
